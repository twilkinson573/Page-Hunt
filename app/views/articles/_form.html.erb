<%= form_for @article do |f| %>
  <% if @article.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@article.errors.count, "error") %> prohibited
      this idea from being posted:</h2>
    <ul>
    <% @article.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  <div class="row">
    <div class="col-lg-3">
      <div class="form-group">
        <%= f.label :Name %>
        <%= f.text_field :title, class: "form-control", placeholder: "Max 20 characters" %>
      </div>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :Description %>
    <%= f.text_field :text, class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.submit "Boom!", class: "btn btn-primary" %>
  </div>
<% end %>